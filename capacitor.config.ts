import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.daytimeblues.aramancia',
  appName: 'Aramancia Tracker',
  webDir: 'dist'
};

export default config;
